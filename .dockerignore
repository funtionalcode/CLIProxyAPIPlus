# Exclude everything by default. Only keep the files Dockerfile copies.
**

# Build instructions
!Dockerfile
!.dockerignore

# Go module metadata
!go.mod
!go.sum

# Runtime template copied into the image
!config.example.yaml

# Build inputs
!cmd/
!cmd/server/
!cmd/server/**
!internal/
!internal/**
!sdk/
!sdk/**
